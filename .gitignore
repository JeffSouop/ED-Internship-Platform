# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local
.env
backend/.env
private.key
data/company-geocode-cache.json

# Conventions Word générées (dossier convention/ à la racine)
convention/*.docx
convention/.conventions-index.json
convention/.convention-docusign.json

# Attestations Word générées (dossier attestation/ à la racine)
attestation/*.docx
attestation/.attestations-index.json

# Ruptures de stage Word générées (dossier rupture/ à la racine)
rupture/*.docx
rupture/.ruptures-index.json

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
COMPANY DATABASE.xlsx
