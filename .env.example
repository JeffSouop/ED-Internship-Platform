# Front (aligné TaTi : tout passe par l’API applicative, pas par Postgres depuis le navigateur)
# — laisser vide : `/api` relatif (proxy Vite en dev, nginx « ui » en Docker).
# — optionnel si backend hors même origine :
# VITE_API_URL=http://localhost:4000

# Backend uniquement : accès Postgres (dev local vers conteneur docker-compose, port hôte 5433)
DATABASE_URL=postgresql://ducasse:ducasse_local_dev@localhost:5433/ducasse_careers
