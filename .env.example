# Développement local (docker-compose.yml à la racine + backend/.env)

# Mot de passe formulaires publics — aligné avec FORM_ACCESS_PASSWORD dans backend/.env
VITE_FORM_ACCESS_PASSWORD=ducasse2026

# Front : laisser vide avec Docker (nginx proxifie /api). Dev Vite seul :
# VITE_API_URL=http://localhost:4000

# ——— Déploiement serveur sans clone : voir .env.dist.example + docker-compose.dist.yml ———
